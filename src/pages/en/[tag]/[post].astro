---
import BaseLayout from "@/layouts/BaseLayout.astro";
import Screen from "@/components/Screen.astro";
import SideNavigation from "@/components/SideNavigation.astro";
import MainContent from "@/components/MainContent.astro";
import MiddleArea from "@/components/MiddleArea.astro";
import RightSidebar from "@/components/RightSidebar.astro";
import WidgetNewsletter from "@/components/WidgetNewsletter.astro";
import WidgetSponsor from "@/components/WidgetSponsor.astro";
import WidgetToc from "@/components/WidgetToc.astro";
import CommentSection from "@/components/CommentSection.astro";
import ShareBtnRow from "@/components/ShareBtnRow.astro";
import {
  getPagesFromDB,
  getPageIdBySlug,
  getPagePropertiesById,
  renderPostById,
} from "@/lib/contentFetcher";

export async function getStaticPaths() {
  const { pages } = await getPagesFromDB();
  return pages.map((page) => ({
    params: { tag: page.tag, post: page.slug },
  }));
}

const { tag, post } = Astro.params;

const pageId = await getPageIdBySlug(post);
if (!pageId) {
  return Astro.redirect("/404");
}

const rendered = await renderPostById(pageId);
if (!rendered) {
  return Astro.redirect("/404");
}
const { Content, headings } = rendered;
const pageProperties = await getPagePropertiesById(pageId);
if (!pageProperties) {
  return Astro.redirect("/404");
}

const postUrl = `https://tsukie.com/en/${tag}/${post}`;
const blogPostingSchema = {
  "@context": "https://schema.org",
  "@type": "BlogPosting",
  headline: pageProperties.title,
  description: pageProperties.summary,
  image: new URL(pageProperties.thumbnail, Astro.site).href,
  datePublished: pageProperties.rawDate,
  url: postUrl,
  author: {
    "@type": "Person",
    name: "Minh Nguyen",
    url: "https://tsukie.com",
  },
  publisher: {
    "@type": "Person",
    name: "Minh Nguyen",
  },
};
---

<BaseLayout
  title={pageProperties.title}
  description={pageProperties.summary}
  ogImage={new URL(pageProperties.thumbnail, Astro.site).href}
  ogUrl={postUrl}
  ogType="article"
  jsonLd={blogPostingSchema}
>
  <Screen>
    <SideNavigation />
    <MainContent>
      <div
        class="grow md:flex space-y-8 md:space-y-0 md:space-x-8 pt-12 md:pt-16 pb-16 md:pb-20"
      >
        <MiddleArea>
          <!-- Back -->
          <div class="mb-3">
            <a
              class="inline-flex text-strong-accent rounded-full border border-border "
              href="/"
            >
              <span class="sr-only">Back</span>
              <svg xmlns="http://www.w3.org/2000/svg" width="34" height="34">
                <path
                  class="fill-current"
                  d="m16.414 17 3.293 3.293-1.414 1.414L13.586 17l4.707-4.707 1.414 1.414z"
                ></path>
              </svg>
            </a>
          </div>

          <article>
            <!-- Post header -->
            <header class="relative zb-card bg-card overflow-hidden p-4">
              <div
                class="absolute inset-0 bg-cover bg-center opacity-50 pointer-events-none"
                style={`background-image: url(${pageProperties.thumbnail})`}
                aria-hidden="true"
              ></div>
              <div class="absolute inset-0 bg-background/75 pointer-events-none" aria-hidden="true"></div>
              <div class="relative z-10">
                <div class="flex items-center justify-between mb-1">
                  <div class="text-xs text-muted-foreground uppercase">
                    <span class="text-strong-accent">&mdash;</span>
                    {pageProperties.date}
                  </div>
                  <ShareBtnRow />
                </div>
                <h1 class="h1 font-aspekta mb-4">{pageProperties.title}</h1>
                <p class="mb-3 text-muted-foreground italic">
                  {pageProperties.summary}
                </p>
              </div>
            </header>
            <hr class="my-4 border-t border-strong-accent" />
            <div class="block md:hidden mb-6">
              <WidgetToc headings={headings} />
            </div>
            <div class="prose  max-w-none">
              <Content />
            </div>
          </article>

          <CommentSection />
        </MiddleArea>

        <RightSidebar>
          <WidgetNewsletter />
          <WidgetSponsor
            title="My latest project"
            pjName="WallCal"
            img="/images/wallcal-screenshot.webp"
            pjDesc="The best sticky calendar on desktop background for macOS"
            link="https://wallcal.app/"
          />
          <WidgetSponsor
            title="My latest project"
            pjName="Yappiely"
            img="/images/yappiely-screenshot.png"
            pjDesc="A lightweight comment platform to engage your readers and grow community"
            link="https://yappiely.com"
          />
          <div slot="sticky" class="hidden md:block">
            <WidgetToc headings={headings} />
          </div>
        </RightSidebar>
      </div>
    </MainContent>
  </Screen>
</BaseLayout>
